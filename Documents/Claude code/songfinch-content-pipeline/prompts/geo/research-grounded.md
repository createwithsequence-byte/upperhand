You are doing GEO (generative engine optimization) research for {{brand_name}}. The goal: get this article cited inside LLM answers when users ask the target prompt.

# Brand: {{brand_name}}

{{brand_context}}

# Thread context

**{{thread_title}}** — {{thread_angle}}

# Article

Target query: "{{target_query}}"
Title: {{article_title}}

# Your job

For an article targeting the prompt "{{target_query}}", search the web and analyze:

1. Which brands currently get cited by ChatGPT, Perplexity, Gemini, and Claude when users ask this prompt? Be specific — name actual brands and which LLMs cite them.
2. What entities, claims, statistics, structures, and formats appear in the content those LLMs are CITING? (Not just ranking — actually quoted or referenced inside LLM answers.)
3. What does {{brand_name}} need to do to win citation? Specific tactics. Reference the actual competitors.

If a prompt is unwinnable for {{brand_name}} — wrong intent, dominant aggregators, brand confusion — say so directly. Better to skip a prompt than to chase it.

Search the web, gather grounded signals, then synthesize.
